DELIMITER $$
CREATE PROCEDURE API1(IN C varchar(50), IN T varchar(50), IN P varchar(50))
BEGIN
    IF C = "Biden" or C = "biden" THEN
		select concat("biden") as result;
	elseif C = "Trump" or C = "trump" then
		select concat("biden") as result;
     end if;
END; $$
DELIMITER ;
